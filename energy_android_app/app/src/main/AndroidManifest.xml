<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ==================== REQUIRED PERMISSIONS ==================== -->
    <!-- Internet permission for M-Pesa API calls and backend communication -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Camera permission for QR code scanning (optional) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Network state permission to check connection -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".EnergyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EnergyApp"
        tools:targetApi="31">

        <!-- ==================== ACTIVITIES ==================== -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ==================== SERVICES ==================== -->
        <!-- M-Pesa background services (if needed) -->

        <!-- ==================== RECEIVERS ==================== -->
        <!-- Network state receiver for connectivity monitoring -->

    </application>

</manifest>